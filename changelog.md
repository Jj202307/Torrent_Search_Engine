# Changelog — Torrent Search Engine

Dated change history of the two research/build sessions. Current-state operational reference (date-free) lives in handover.md; day-by-day working notes live in work_trace_log.md.

## Session 2026-08-01 — initial research & 16-scraper build

Initial research pass ranked torrent sites by ~count. Verified count: **RuTracker = 2.7M torrents** (Wikipedia, May 2026); all others are self-reported estimates. Sites removed by user from the final list: Nyaa.si, Pornolab.net, AnimeTosho, Skidrowreloaded, FitGirl Repacks, idope.se, BTDB. Excluded by criteria (paid/invite-only): IPTorrents, AnimeBytes, TorrentLeech.

Built the first 16 scrapers (parallel async, common `SearchResult` schema, client-side filter engine). Verification results: TPB `alive(): True` ("ubuntu" → 50 results); YTS `alive(): True` after the API fix ("avatar" 1080p min-seeders 10 → 3 results); all 16 scrapers import cleanly and `--list-sources` shows all 16; multi-source `"debian" --sources tpb,yts` returns combined filtered results.

Bugs found & fixed:
1. **YTS API moved**: old `yts.bz/api/v2` → 301. Fixed `config.py` `yts_api` → `https://movies-api.accel.li/api/v2`. Verified alive, returns torrents.
2. **FilterEngine `_check_pattern` case bug**: `pattern_dict.get(key.upper())` failed because dict keys are lowercase (`1080p` vs `1080P`). Fix: `pattern_dict.get(key) or pattern_dict.get(key.upper()) or pattern_dict.get(key.lower())`. Quality/codec filters were silently returning 0 results before this fix.
3. **Rich table clipping**: Seeders/Leechers columns appeared empty in bash-captured output — terminal-width capture clipping, not a code bug. Use `--format simple` for narrow terminals.

## Session 2026-09-06 — 16 → 19 scrapers, RuTracker bridge, filters, pagination, browse

- **16 → 19 scrapers**: added yourbittorrent.com, knaben.org, audiobookbay.lu; **snowfl.com dropped** (SPA with rotating-key JSON API — key vars no longer extractable from b.min.js). Site notes: yourbittorrent rows carry the same injected t0r.space spam as TorLock (filtered by relative-href); knaben `?query=` form is a status-dashboard stub, the real route is `/search/{query}`; audiobookbay is TCP-unreachable from this network (geo), verified via relay.
- **Liveness sweep**: yts.mx dead (stopped Jan 2026 — scraper already on yts.bz + movies-api.accel.li, both live; yts.gg = current official gateway), torrentgalaxy.to dead (memecoin promo), glodls dead, zooqle.to dead, btdb.to dead, 7torrents dead, torrents.io dead, torrent.by dead, ilcorsaronero.info dead (hijacked), rarbg family dead (2023). Domain note: yourbitorrent.com (dead) ≠ yourbittorrent.com (live).
- Bugs found & fixed:
  4. **TorLock parser**: first-`<a>`-in-row picked the category link (or spam mirror link) as title; positional columns off by one; `torrent_url` was an HTML page (BiglyBT fetched HTML, silently dropped). Fix: `a.tl-name[href]` anchor, `td.ts/tul/tdl` classes, real `.torrent` at `lt.t0r.space/tor/{id}.torrent`.
  5. **BiglyBT flatpak runner drops ALL CLI args** (root cause of "opens but nothing loads"): `/var/lib/flatpak/app/com.biglybt.BiglyBT/…/files/bin/biglybt-runner` is `exec /app/biglybt/biglybt` with no `"$@"`. Fix in `download.py`: launch via `flatpak run --command=/app/biglybt/biglybt com.biglybt.BiglyBT` (main script forwards args correctly). Upstream flathub bug.
  6. **Cold-start race**: client launched with a URI isn't ready to receive it. Fix in `download.py`: `_READY_PROBES` (127.0.0.1:6880), launch first URI → poll listener up to 60s → resend all URIs (client dedupes), 0.25s stagger.
  7. **Magnet conversion flakiness** ("doesn't appear to be valid torrent file"): UA-less single-attempt fetch of `.torrent` mirrors failed under burst throttling. Fix: `ensure_magnet` now sends `DEFAULT_USER_AGENT`, 3 attempts with backoff, bencode validation; summary marks `(direct URL — magnet conversion failed)` fallbacks.
- **Download pipeline hardening** (details now in handover.md: magnet-building mechanics, `fetch_torrent_magnet` routing for rutracker, flatpak data paths, alt-client matrix).
- **RuTracker Firefox cookie bridge arrival**: harvest `bb_session` + `cf_clearance` from `~/.mozilla/firefox/*/cookies.sqlite` before every run; replay via curl_cffi `impersonate="firefox135"` with UA matched to the cookie-issuing browser. Key findings: checkbox-solved clearances replay, invisible auto-rotated ones do not; request volume burns the clearance (search reduced to exactly 1 request); forum index is not CF-guarded; `FIREFOX_UA` bound to cf_clearance. Dead ends proven (httpx/curl_cffi direct 403, playwright/patchright, turnstile-iframe click, Marionette). Latent `self._decode(...)` AttributeError (swallowed by bare except → always `[]`) fixed en route.
- **Filter**: `QUALITY_ALIASES` {4k→2160p, uhd→2160p, fhd→1080p} + value negation `-q '!480p'` implemented in `FilterEngine._check_pattern` (leading `!` marks an exclude; covers quality/codec/source-type/hdr uniformly). `-qx 480p` argparse syntax is impossible — value negation chosen over a rejected `-x` argv preprocessor.
- **Pagination** added: `--page N` slices the merged/filtered/sorted list; `config.set_max_results_per_source` patches each scraper module's import-bound constant; per-source depth bound noted.
- **RuTracker -C/-F/-P/-L scope flags + browse mode** added: named presets (`f=` narrowing), repeatable raw `-F` ids, server pages `-P 1..10` of 50 (5s spacing), `-L` directory listing. Empty query + rt flag = browse mode (fixed en route: argparse `query` is `None`, not `""`). Live-verified: repeated `f=` ANDs forums; deep pages ride `search_id=<token>`; hard cap 500 rows. `hires` preset renamed `hi-res`; `movies` (30) and `tv-series` (40) added same day. Findings: `tracker.php?f=` NOT recursive; browse pages arrive forum-major with `s`/`o` sort ignored; `nm=` doesn't index the `[year]` bracket; **query × multi-f= loss** (nm= + many f= silently loses matches → rule: query + 1-2 `-F` ids, or browse-only presets); CLI hygiene fixes (per-source timeout in `search_single`, stderr failure lines, `--page` past end warns); silent-exit paths in `rutracker.search()` noted.
- **-h epilog rutracker docs + forum code tables**: `-h` now ends with a rutracker section via `_HELP_EPILOG` (`epilog=` + `RawDescriptionHelpFormatter`): preset table with forum counts, -F/-P/-L explanations, the search-shape tip, and 11 examples. HowToUse.md §4 mirrors these. Commit **b4096ab** ("1").
- **movies/tv-series presets REBUILT via live tree walk**: walked the live branch tree (general subagent, 15 requests, 241 forums mapped → /tmp/opencode/tree_walk.json). KEY discovery: plain index.php TRUNCATES subforum lists — `index.php?c=N` category pages are the complete source. Found **rutracker RECYCLES dead forum ids** (32, 91, 1417→Breaking Bad, 1214, 1359, 704, 781, 1301, 1539, 1574, 915, 1242, 2102, 2412 all repointed). US & Canada forums (235/266/1669) were MISSING → resolved. movies 30→106, tv-series 40→78; hi-res/digitizations verified 16/16 untouched; epilog counts synced; -C label updated. Commit **b0cef61**.
- **No-keyword browse feature + YTS internal pagination + eztvx beta API browse**: `BaseScraper.supports_browse` flag (yts+eztvx True; rutracker stays False); CLI empty-query gate; launcher fixed so onboarding only runs when `$# -eq 0`. YTS: limit>50 clamps to 20, internal pagination (PAGE_SIZE 50 / MAX_PAGES 20 / 0.35s / dedupe (movie_id, torrent_hash) / past-end = `movies` key absent). eztvx: RSS is a hard 30-item buffer with all params ignored, /releases/ CF-403, beta API `GET /api/get-torrents` becomes the primary browse source. Verdict: no server-side TV-popularity sort anywhere. Commit **dbea349**.

### Commit chain

74a32e5 (rutracker browse docs/examples) → 8e236fa (rename torrent-search → torrent_search_dl in user-facing text) → 717c832 (replace zero-row example queries with verified shapes) → b0cef61 (rebuild movies/tv-series presets from live tree walk) → b4096ab (forum-id reference tables in -h epilog) → dbea349 (no-keyword yts/eztvx browse, yts pagination fix, eztvx beta API). Current HEAD: **dbea349**, pushed.
